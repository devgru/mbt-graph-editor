import React, {Component} from 'react';

import './Failed.css';

class Failed extends Component {
  render() {
    return (
      <div className="Failed">Сервер не отвечает.</div>
    );
  }
}

export default Failed;
